[project]
name = "eps-2026-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ipykernel>=7.2.0",
    "pyrokinetics",
]

[tool.uv.sources]
pyrokinetics = { git = "https://github.com/pyro-kinetics/pyrokinetics.git" }
